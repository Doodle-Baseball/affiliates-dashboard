{
  "name": "affiliates-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Unified affiliate earnings dashboard — scrapes five affiliate portals and shows today's numbers on one local page.",
  "engines": { "node": ">=20.11" },
  "scripts": {
    "setup": "node src/scripts/setup.js",
    "migrate": "node src/scripts/migrate.js",
    "discover": "node src/scripts/discover.js",
    "sync": "node src/scripts/sync.js",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "playwright": "^1.50.1"
  },
  "devDependencies": {
    "vitest": "^3.0.5"
  }
}
